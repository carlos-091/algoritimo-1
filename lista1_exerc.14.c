#include <stdio.h>
  int main()
  {
  int num;
  printf("digiter um numero:");
  scanf("%d",&num);

  if(num %2==0) {
  printf("\n e par");
  }
  if(num %2!=0) {
  printf("\n e impar");
  }
   return 0;
  }
